<UserControl x:Class="TWChatOverlay.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <UserControl.Resources>
        <!-- 왼쪽 내비게이션 항목: 선택 시 왼쪽 2px 민트 라인 + 밝은 텍스트 -->
        <Style x:Key="SettingsNavItemStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource OverlaySubtleTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="2,0,0,0"
                                Padding="22,6">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource OverlayTabHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource OverlayTabSelectedBorderBrush}"/>
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource OverlayTabSelectedBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 내비게이션 그룹 라벨: 상위 구분 제목이므로 항목보다 크고 굵게 -->
        <Style x:Key="SettingsNavGroupLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="14,14,0,6"/>
        </Style>

        <!-- 섹션 라벨: 작은 회색 구분 텍스트 -->
        <Style x:Key="SettingsSectionLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource OverlayHintTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,14,0,6"/>
        </Style>

        <!-- 설정 행: 전 탭 공통 전체 폭 — 라벨·설명은 왼쪽, 토글·버튼은 오른쪽 끝(606)에 정렬 -->
        <Style x:Key="SettingsRowStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource SeparatorBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="0,7"/>
            <Setter Property="Width" Value="606"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <!-- 전체 폭 행 (기본 행과 동일 — 호환용 별칭) -->
        <Style x:Key="SettingsRowWideStyle" TargetType="Border" BasedOn="{StaticResource SettingsRowStyle}"/>

        <!-- 색상 동기화 링크형 체크: 켜짐=민트 동기화, 꺼짐=회색 -->
        <Style x:Key="SyncLinkCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="ToolTip" Value="색상 동기화 — 켜면 줄 색을 따르고, 끄면 색 버튼으로 직접 지정"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <TextBlock x:Name="Txt" Text="동기화" FontSize="11" Foreground="{DynamicResource OverlaySubtleTextBrush}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Txt" Property="Foreground" Value="{DynamicResource OverlayAccentTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 행 왼쪽 라벨 -->
        <Style x:Key="SettingsRowLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource OverlayInfoTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- 행 라벨 아래 설명 -->
        <Style x:Key="SettingsRowHintStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource OverlayHintTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,2,12,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- 행 오른쪽 입력칸 -->
        <Style x:Key="SettingsRowInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputFieldBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputFieldBorderBrush}"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Width" Value="90"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <!-- 작은 보조 버튼 -->
        <Style x:Key="DropFilterButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="MinWidth" Value="34"/>
        </Style>

        <!-- 레벨별 색상 스펙트럼 바의 한 칸: 배경=그 구간의 색, 클릭하면 색 변경 -->
        <Style x:Key="EtaRangeSegmentStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#111418"/>
            <Setter Property="ToolTip" Value="클릭하면 이 구간의 색을 바꿉니다"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Seg" Background="{TemplateBinding Background}" BorderBrush="#66000000" BorderThickness="0,0,1,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Seg" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Seg" Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 도움말 [?] 버튼: Tag에 도움말 키를 지정, Help_Click에서 HelpWindow 표시 -->
        <Style x:Key="HelpButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Margin" Value="7,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ToolTip" Value="설명 보기"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="Transparent"
                                BorderBrush="{DynamicResource OverlayHintTextBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <TextBlock x:Name="Glyph"
                                       Text="?"
                                       FontSize="10"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource OverlayHintTextBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Margin="0,-1,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource OverlayAccentTextBrush}"/>
                                <Setter TargetName="Glyph" Property="Foreground" Value="{DynamicResource OverlayAccentTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 도구 버튼(사이드바 최상단): 즉시 실행 항목 — 테두리 있는 버튼 칩 (EllesmereUI 참고) -->
        <Style x:Key="SettingsNavActionStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource OverlayInfoTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{DynamicResource OverlayCardBackgroundBrush}"
                                BorderBrush="{DynamicResource ControlBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Margin="8,2"
                                Padding="10,6">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource OverlayTabHoverBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource OverlayTabSelectedBorderBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource OverlayTabSelectedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 도구 라디오(프로필 및 프리셋): 버튼 칩과 같은 형태 + 선택 상태 -->
        <Style x:Key="SettingsNavToolRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource OverlayInfoTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd"
                                Background="{DynamicResource OverlayCardBackgroundBrush}"
                                BorderBrush="{DynamicResource ControlBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Margin="8,2"
                                Padding="10,6">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource OverlayTabHoverBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource OverlayTabSelectedBorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource OverlayTabSelectedBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource OverlayTabSelectedBorderBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 패널 상단 섹션 탭 (EllesmereUI 스타일: 선택 시 민트 밑줄) -->
        <Style x:Key="SettingsTabStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource OverlaySubtleTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="12,6">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource OverlayTabSelectedBorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 탭 스트립 컨테이너: 아래 구분선 -->
        <Style x:Key="SettingsTabStripStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource SeparatorBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Margin" Value="0,8,0,12"/>
        </Style>
    </UserControl.Resources>

    <Border x:Name="RootBorder" Background="Transparent" BorderThickness="0" Padding="8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 도구: 사이드바 최상단 버튼 그룹 (EllesmereUI 스타일) -->
            <Border x:Name="NavToolsColumn"
                    Grid.Row="0" Grid.Column="0"
                    Background="{DynamicResource InputFieldBackgroundBrush}"
                    BorderBrush="{DynamicResource SeparatorBrush}"
                    BorderThickness="0,0,1,0"
                    Width="168">
                <StackPanel Margin="0,8,0,0">
                    <Button Style="{StaticResource SettingsNavActionStyle}" Click="UnlockMode_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE785;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="잠금 해제 모드" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SettingsNavActionStyle}" Click="NavWizard_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="설정 마법사" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SettingsNavActionStyle}" Click="NavPatchNotes_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE70B;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="패치 노트" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <RadioButton x:Name="NavPresets" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavToolRadioStyle}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="프로필" VerticalAlignment="Center"/>
                        </StackPanel>
                    </RadioButton>
                    <Button Style="{StaticResource SettingsNavActionStyle}" Command="{Binding ExitAppCommand}" ToolTip="프로그램을 종료합니다">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="프로그램 종료" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Border Height="1" Background="{DynamicResource SeparatorBrush}" Margin="8,8,8,0"/>
                </StackPanel>
            </Border>

            <!-- 도구 오른쪽: 프로그램 아이콘 + 이름 (본문은 아래 행에서 시작) -->
            <Border x:Name="AppHeader" Grid.Row="0" Grid.Column="1">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="48,10,0,10">
                    <Border CornerRadius="4" ClipToBounds="True">
                        <Image Source="/TWChatOverlay;component/favicon.ico" Width="128" Height="128" VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    </Border>
                    <TextBlock Text="테일즈 채팅 오버레이" Foreground="{DynamicResource TextBrush}" FontSize="36" FontWeight="Bold"
                               VerticalAlignment="Center" Margin="28,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- 왼쪽 내비게이션 (컴팩트 모드에서는 숨김) -->
            <Border x:Name="NavColumn"
                    Grid.Row="1" Grid.Column="0"
                    Background="{DynamicResource InputFieldBackgroundBrush}"
                    BorderBrush="{DynamicResource SeparatorBrush}"
                    BorderThickness="0,0,1,0"
                    Width="168">
                <StackPanel Margin="0,4">
                    <TextBlock Text="기본" Style="{StaticResource SettingsNavGroupLabelStyle}"/>
                    <RadioButton x:Name="NavChat" Content="채팅" GroupName="SettingsNav" IsChecked="True" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavShout" Content="외치기" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavDisplay" Content="화면" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>

                    <TextBlock Text="추가 기능" Style="{StaticResource SettingsNavGroupLabelStyle}"/>
                    <RadioButton x:Name="NavAddonKeyword" Content="키워드 알림" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavAddonExp" Content="경험치 추적" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavAddonDungeon" Content="던전 도우미" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavAddonItem" Content="아이템 알림" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavAddonBuff" Content="버프 추적" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavAddonBoss" Content="필드 보스" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>

                    <TextBlock Text="관리" Style="{StaticResource SettingsNavGroupLabelStyle}"/>
                    <RadioButton x:Name="NavHotkeys" Content="단축키" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                    <RadioButton x:Name="NavSystem" Content="시스템" GroupName="SettingsNav" Checked="Nav_Checked" Style="{StaticResource SettingsNavItemStyle}"/>
                </StackPanel>
            </Border>

            <ScrollViewer x:Name="SettingsScroll" Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto" Style="{StaticResource OverlayScrollViewerStyle}">
                <StackPanel Margin="16,4,12,12" Width="620" HorizontalAlignment="Left">

                    <!-- ===================== 채팅 ===================== -->
                    <StackPanel x:Name="ChatPanel">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="ChatTabFilter" Content="채팅 필터" IsChecked="True" GroupName="ChatTabs" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="ChatTabId" Content="채팅 표시" GroupName="ChatTabs" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="ChatTabFont" Content="폰트" GroupName="ChatTabs" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="ChatTabClone1" Content="서브 채팅창 1" GroupName="ChatTabs" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="ChatTabClone2" Content="서브 채팅창 2" GroupName="ChatTabs" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ChatTabFilter, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="기본 채팅 필터" Style="{StaticResource SettingsSectionLabelStyle}" Margin="0"/>
                            <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.filter" Click="Help_Click"/>
                        </StackPanel>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding NormalColor}" Command="{Binding ColorPickCommand}" CommandParameter="Normal" Style="{StaticResource ColorPickerButtonStyle}"/>
                                        <CheckBox IsChecked="{Binding ShowNormal}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <TextBlock Text="일반" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding TeamColor}" Command="{Binding ColorPickCommand}" CommandParameter="Team" Style="{StaticResource ColorPickerButtonStyle}"/>
                                        <CheckBox IsChecked="{Binding ShowTeam}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <TextBlock Text="팀" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding ClubColor}" Command="{Binding ColorPickCommand}" CommandParameter="Club" Style="{StaticResource ColorPickerButtonStyle}"/>
                                        <CheckBox IsChecked="{Binding ShowClub}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <TextBlock Text="클럽" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding FreeShoutColor}" Command="{Binding ColorPickCommand}" CommandParameter="ShoutFree" Style="{StaticResource ColorPickerButtonStyle}"/>
                                        <CheckBox IsChecked="{Binding ShowFreeShout}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <TextBlock Text="외치기 · 무료 (From)" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="줄 끝이 'From [이름]'인 외치기"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding PaidShoutColor}" Command="{Binding ColorPickCommand}" CommandParameter="ShoutPaid" Style="{StaticResource ColorPickerButtonStyle}"/>
                                        <CheckBox IsChecked="{Binding ShowPaidShout}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <TextBlock Text="외치기 · 유료 (Click)" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="줄 끝이 'Click [이름]'인 외치기"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding NoticeShoutColor}" Command="{Binding ColorPickCommand}" CommandParameter="ShoutNotice" Style="{StaticResource ColorPickerButtonStyle}"/>
                                        <CheckBox IsChecked="{Binding ShowNoticeShout}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <TextBlock Text="외치기 · 공지" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="From/Click 꼬리표가 없는 외치기 — 룬 마스터 달성, 강화 성공 같은 시스템 공지"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding SystemColor}" Command="{Binding ColorPickCommand}" CommandParameter="System" Style="{StaticResource ColorPickerButtonStyle}"/>
                                        <CheckBox IsChecked="{Binding ShowSystem}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <TextBlock Text="시스템" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding ShowCategoryPrefix}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="말머리" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.filter.category_prefix" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ChatTabId, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="채팅 표시" Style="{StaticResource SettingsSectionLabelStyle}" Margin="0,0,0,6"/>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Style="{StaticResource SyncLinkCheckBoxStyle}" IsChecked="{Binding SenderIdColorSync}"/>
                                        <Button Width="26" Height="14" Margin="0,0,62,0" Background="{Binding SenderIdColor}" IsEnabled="{Binding SenderIdColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="SenderId" Style="{StaticResource ColorPickerButtonStyle}" ToolTip="동기화를 끄면 지정할 수 있습니다"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="아이디" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="채팅 줄의 보낸 사람 아이디 색상"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.id.sender" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Style="{StaticResource SyncLinkCheckBoxStyle}" IsChecked="{Binding EtaLevelColorSync}" ToolTip="색상 동기화 — 켜면 줄 색을 따르고, 끄면 아래 레벨별 색을 사용"/>
                                        <CheckBox IsChecked="{Binding ShowEtaLevel}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="에타 레벨" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.id.eta_level" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <!-- 에타 레벨의 소분류: 동기화 해제 시 사용되는 레벨별(구간) 색 -->
                            <Border Style="{StaticResource SettingsRowWideStyle}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                                        <TextBlock Text="레벨별 색상" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="에타 레벨 '동기화'를 끄면 구간에 따라 이 색이 적용됩니다"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.id.eta_range" Click="Help_Click"/>
                                    </StackPanel>
                                    <Border Margin="28,8,0,2" HorizontalAlignment="Left" BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="1" CornerRadius="3">
                                        <UniformGrid Columns="5" Width="440" Height="22">
                                            <Button Content="1~20" Background="{Binding EtaLevelRange1Color}" IsEnabled="{Binding EtaLevelColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="EtaLevelRange1" Style="{StaticResource EtaRangeSegmentStyle}"/>
                                            <Button Content="21~40" Background="{Binding EtaLevelRange2Color}" IsEnabled="{Binding EtaLevelColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="EtaLevelRange2" Style="{StaticResource EtaRangeSegmentStyle}"/>
                                            <Button Content="41~60" Background="{Binding EtaLevelRange3Color}" IsEnabled="{Binding EtaLevelColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="EtaLevelRange3" Style="{StaticResource EtaRangeSegmentStyle}"/>
                                            <Button Content="61~80" Background="{Binding EtaLevelRange4Color}" IsEnabled="{Binding EtaLevelColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="EtaLevelRange4" Style="{StaticResource EtaRangeSegmentStyle}"/>
                                            <Button Content="81~" Background="{Binding EtaLevelRange5Color}" IsEnabled="{Binding EtaLevelColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="EtaLevelRange5" Style="{StaticResource EtaRangeSegmentStyle}"/>
                                        </UniformGrid>
                                    </Border>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Style="{StaticResource SyncLinkCheckBoxStyle}" IsChecked="{Binding EtaCharacterColorSync}"/>
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding EtaCharacterColor}" IsEnabled="{Binding EtaCharacterColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="EtaCharacter" Style="{StaticResource ColorPickerButtonStyle}" ToolTip="동기화를 끄면 지정할 수 있습니다"/>
                                        <CheckBox IsChecked="{Binding ShowEtaCharacter}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="캐릭터" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.id.character" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Style="{StaticResource SyncLinkCheckBoxStyle}" IsChecked="{Binding IdTagColorSync}"/>
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding IdTagColor}" IsEnabled="{Binding IdTagColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="IdTag" Style="{StaticResource ColorPickerButtonStyle}" ToolTip="동기화를 끄면 지정할 수 있습니다"/>
                                        <CheckBox IsChecked="{Binding ShowIdTag}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="아이디 태그" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="idtag.txt의 메모를 아이디 뒤에 [태그]로 표시"/>
                                        <Button Click="OpenIdTagFile_Click" ToolTip="idtag.txt 편집" Cursor="Hand" Margin="8,0,0,0" Padding="0" Background="Transparent" BorderThickness="0" VerticalAlignment="Center">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <TextBlock x:Name="Txt" Text="편집" FontSize="11" Foreground="{DynamicResource OverlaySubtleTextBrush}" TextDecorations="Underline"/>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Txt" Property="Foreground" Value="{DynamicResource OverlayAccentTextBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.id.id_tag" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Style="{StaticResource SyncLinkCheckBoxStyle}" IsChecked="{Binding ClubBossColorSync}"/>
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding ClubBossColor}" IsEnabled="{Binding ClubBossColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="ClubBoss" Style="{StaticResource ColorPickerButtonStyle}" ToolTip="동기화를 끄면 지정할 수 있습니다"/>
                                        <CheckBox IsChecked="{Binding ShowClubBoss}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="클럽 보스" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.id.club_boss" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Style="{StaticResource SyncLinkCheckBoxStyle}" IsChecked="{Binding TimestampColorSync}"/>
                                        <Button Width="26" Height="14" Margin="0,0,10,0" Background="{Binding TimestampColor}" IsEnabled="{Binding TimestampColorEditable}" Command="{Binding ColorPickCommand}" CommandParameter="Timestamp" Style="{StaticResource ColorPickerButtonStyle}" ToolTip="동기화를 끄면 지정할 수 있습니다"/>
                                        <CheckBox IsChecked="{Binding ShowTimestamp}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="타임 스탬프" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.id.timestamp" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ChatTabFont, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="폰트" Style="{StaticResource SettingsSectionLabelStyle}" Margin="0"/>
                            <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.font" Click="Help_Click"/>
                        </StackPanel>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowWideStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <ComboBox x:Name="FontFamilyCombo" Width="170" Height="26"
                                                  SelectionChanged="FontFamilyCombo_SelectionChanged"
                                                  Style="{StaticResource DarkComboBoxStyle}"
                                                  ToolTip="'사용자 설정'은 Font\UserDefine.ttf(.otf/.ttc) 파일을 사용합니다"/>
                                    </StackPanel>
                                    <TextBlock Text="종류" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}" BorderThickness="0">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="40" Value="{Binding FontSize}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                        <TextBlock Text="{Binding FontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ChatTabClone1, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="서브 채팅창 1" Style="{StaticResource SettingsSectionLabelStyle}" Margin="0"/>
                            <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.clone1" Click="Help_Click"/>
                        </StackPanel>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Clone1Enabled}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <TextBlock Text="사용" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Clone1FollowMainFont}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <TextBlock Text="메인 폰트 따라가기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <ComboBox DockPanel.Dock="Right" Width="150" Height="26"
                                              ItemsSource="{Binding AvailableFonts}"
                                              SelectedItem="{Binding Clone1FontFamily, Mode=TwoWay}"
                                              IsEnabled="{Binding Clone1FollowMainFont, Converter={StaticResource BooleanInverseConverter}}"
                                              Style="{StaticResource DarkComboBoxStyle}"/>
                                    <TextBlock Text="폰트" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="28" Value="{Binding Clone1FontSize, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"
                                                IsEnabled="{Binding Clone1FollowMainFont, Converter={StaticResource BooleanInverseConverter}}"/>
                                        <TextBlock Text="{Binding Clone1FontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ChatTabClone2, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="서브 채팅창 2" Style="{StaticResource SettingsSectionLabelStyle}" Margin="0"/>
                            <Button Style="{StaticResource HelpButtonStyle}" Tag="chat.clone2" Click="Help_Click"/>
                        </StackPanel>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Clone2Enabled}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <TextBlock Text="사용" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Clone2FollowMainFont}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <TextBlock Text="메인 폰트 따라가기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <ComboBox DockPanel.Dock="Right" Width="150" Height="26"
                                              ItemsSource="{Binding AvailableFonts}"
                                              SelectedItem="{Binding Clone2FontFamily, Mode=TwoWay}"
                                              IsEnabled="{Binding Clone2FollowMainFont, Converter={StaticResource BooleanInverseConverter}}"
                                              Style="{StaticResource DarkComboBoxStyle}"/>
                                    <TextBlock Text="폰트" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="28" Value="{Binding Clone2FontSize, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"
                                                IsEnabled="{Binding Clone2FollowMainFont, Converter={StaticResource BooleanInverseConverter}}"/>
                                        <TextBlock Text="{Binding Clone2FontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- ===================== 외치기 ===================== -->
                    <StackPanel x:Name="ShoutPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="외치기 알림" IsChecked="True" GroupName="ShoutTabs" IsHitTestVisible="False" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding ShowShoutToastPopup}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="외치기 팝업" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="외치기 로그가 들어오면 토스트 팝업 표시"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="shout.toast" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding AutoCopyShoutNickname}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="닉네임 자동복사" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="외치기 끝의 [닉네임]을 클립보드에 복사"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="shout.autocopy" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="70" Text="{Binding ShoutToastDurationSeconds, UpdateSourceTrigger=LostFocus}" Style="{StaticResource SettingsRowInputStyle}" PreviewTextInput="NumberOnly_PreviewTextInput" KeyDown="OffsetInput_KeyDown"/>
                                    <TextBlock Text="유지 시간(초)" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="40" Value="{Binding ShoutToastFontSize}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                        <TextBlock Text="{Binding ShoutToastFontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="토스트 글자 크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="shout.toast_font" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>

                    <!-- ===================== 화면 ===================== -->
                    <StackPanel x:Name="DisplayPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="오버레이" IsChecked="True" GroupName="DisplayTabs" IsHitTestVisible="False" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="20" Maximum="100" Value="{Binding OverlayOpacityPercent, Mode=TwoWay, FallbackValue=96}" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                        <TextBlock Text="{Binding OverlayOpacityPercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="투명도" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="글자는 항상 선명하게 유지됩니다"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="display.opacity" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Content="잠금 해제" Width="74" Height="26" Padding="0" Foreground="White" Click="UnlockMode_Click" Style="{StaticResource SimButtonStyle}" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="잠금 해제 모드" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="이 모드에서만 창을 드래그해 이동할 수 있습니다"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="display.unlock" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding MenuWindowHorizontal}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="메뉴 바 가로형" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="display.menu_horizontal" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding OverlaysAlwaysOnTop}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="항상 위" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="display.always_on_top" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding AttachOverlaysToGameWindow}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="게임 창에 부착" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="display.attach_to_game_window" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding UnifiedToastStack}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="알림 표시 위치 통합" Style="{StaticResource SettingsRowLabelStyle}"
                                                   ToolTip="켜면 외치기·던전·경험치·아이템·필드 보스 알림이 한 위치에 쌓이고, 끄면 종류별 위치에 각각 표시됩니다"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="display.toast_unified" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>

                    <!-- ===================== 단축키 ===================== -->
                    <StackPanel x:Name="HotkeyPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="일반" IsChecked="True" GroupName="HotkeyTabs" IsHitTestVisible="False" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>
                        <TextBlock Text="칸을 클릭한 뒤 원하는 키 조합을 누르세요. Esc 또는 Delete로 해제합니다." Style="{StaticResource SettingsRowHintStyle}" Margin="0,0,0,6"/>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="120" Text="{Binding ToggleOverlayHotKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}" PreviewKeyDown="HotKeyInput_PreviewKeyDown" PreviewMouseLeftButtonDown="HotKeyTextBox_PreviewMouseLeftButtonDown" IsReadOnly="True" Tag="HotKey"/>
                                    <TextBlock Text="채팅창 ON/OFF" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="120" Text="{Binding ToggleDailyWeeklyContentHotKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}" PreviewKeyDown="HotKeyInput_PreviewKeyDown" PreviewMouseLeftButtonDown="HotKeyTextBox_PreviewMouseLeftButtonDown" IsReadOnly="True" Tag="HotKey"/>
                                    <TextBlock Text="일일/주간 컨텐츠" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="120" Text="{Binding ToggleSettingsHotKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}" PreviewKeyDown="HotKeyInput_PreviewKeyDown" PreviewMouseLeftButtonDown="HotKeyTextBox_PreviewMouseLeftButtonDown" IsReadOnly="True" Tag="HotKey"/>
                                    <TextBlock Text="설정창 ON/OFF" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="120" Text="{Binding ToggleTrayAllHotKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}" PreviewKeyDown="HotKeyInput_PreviewKeyDown" PreviewMouseLeftButtonDown="HotKeyTextBox_PreviewMouseLeftButtonDown" IsReadOnly="True" Tag="HotKey"/>
                                    <TextBlock Text="모든 창 트레이" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="120" Text="{Binding ToggleUnlockHotKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}" PreviewKeyDown="HotKeyInput_PreviewKeyDown" PreviewMouseLeftButtonDown="HotKeyTextBox_PreviewMouseLeftButtonDown" IsReadOnly="True" Tag="HotKey"/>
                                    <TextBlock Text="잠금 해제 모드" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="120" Text="{Binding ExitHotKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}" PreviewKeyDown="HotKeyInput_PreviewKeyDown" PreviewMouseLeftButtonDown="HotKeyTextBox_PreviewMouseLeftButtonDown" IsReadOnly="True" Tag="HotKey"/>
                                    <TextBlock Text="프로그램 종료" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>

                    <!-- ===================== 시스템 ===================== -->
                    <StackPanel x:Name="SystemPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="SystemTabLog" Content="로그" IsChecked="True" GroupName="SystemTabs" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="SystemTabMaintain" Content="유지 관리" GroupName="SystemTabs" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=SystemTabLog, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowWideStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="240" Text="{Binding ChatLogFolderPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" Style="{StaticResource SettingsRowInputStyle}" TextAlignment="Left" Padding="5,0"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="채팅 로그 폴더" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="system.log_folder" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border x:Name="DebugOptionsBorder" Style="{StaticResource SettingsRowWideStyle}" Visibility="Collapsed">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding EnableDebugLogging}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="Debug.log 활성화" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="디버그 로그를 Debug.log 파일에 기록"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="system.debug_log" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <!-- 디버그 빌드 전용: 필드 보스 알림 테스트 발사 (실제 사운드+팝업 흐름) -->
                            <Border x:Name="BossAlertTestBorder" Style="{StaticResource SettingsRowWideStyle}" Visibility="Collapsed">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <ComboBox x:Name="BossAlertTestCombo" Width="110" Height="26" Style="{StaticResource DarkComboBoxStyle}">
                                            <ComboBoxItem Content="아칸" Tag="Arkan" IsSelected="True"/>
                                            <ComboBoxItem Content="스페르첸드" Tag="Scherzendo"/>
                                            <ComboBoxItem Content="파멸의 기원" Tag="Origin of Doom"/>
                                            <ComboBoxItem Content="혼란한 대지" Tag="Confused Land"/>
                                            <ComboBoxItem Content="이벤트" Tag="event"/>
                                        </ComboBox>
                                        <Button Content="3분 전" Tag="3분 전" Width="56" Height="26" Padding="0" Margin="6,0,0,0" Click="BossAlertTest_Click" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                                        <Button Content="1분 전" Tag="1분 전" Width="56" Height="26" Padding="0" Margin="6,0,0,0" Click="BossAlertTest_Click" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                                        <Button Content="5초 전" Tag="5초 전" Width="56" Height="26" Padding="0" Margin="6,0,0,0" Click="BossAlertTest_Click" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="필드 보스 알림 테스트" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="선택한 보스의 실제 알림(사운드+팝업)을 즉시 발사"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=SystemTabMaintain, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Content="실행" Width="70" Height="26" Padding="0" Command="{Binding ManualUpdateCommand}" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="수동 업데이트" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="system.manual_update" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Content="실행" Width="70" Height="26" Padding="0" Command="{Binding ManualLogReloadCommand}" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="로그 다시 읽기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="system.log_reload" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Content="초기화" Width="70" Height="26" Padding="0" Command="{Binding InitSettingsCommand}" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="설정 초기화" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="모든 설정을 기본값으로 되돌립니다"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="system.reset" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- ===================== 프로필 ===================== -->
                    <StackPanel x:Name="PresetPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="프로필" IsChecked="True" GroupName="ProfileTabs" IsHitTestVisible="False" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>
                        <StackPanel x:Name="ProfileListPanel"/>
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,16">
                            <Button Content="+ 새 프로필 추가" Width="140" Height="26" Padding="0"
                                    Click="AddProfile_Click" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                            <Button Content="파일에서 불러오기" Width="130" Height="26" Padding="0" Margin="8,0,0,0"
                                    Click="ImportProfileFile_Click" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"
                                    ToolTip="내보낸 프로필(.json) 파일을 골라 현재 설정에 적용합니다"/>
                            <Button Content="파일로 내보내기" Width="120" Height="26" Padding="0" Margin="8,0,0,0"
                                    Click="ExportProfileFile_Click" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"
                                    ToolTip="현 시점의 모든 설정을 프로필 파일로 저장합니다"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- ===================== 추가 기능: 키워드 알림 ===================== -->
                    <StackPanel x:Name="AddonKeywordPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="KeywordTabAlert" Content="키워드 알림" IsChecked="True" GroupName="KeywordTabs" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="KeywordTabList" Content="키워드" GroupName="KeywordTabs" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=KeywordTabAlert, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.UseAlertColor}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="색상 강조" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="keyword.color" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.UseAlertSound}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <TextBlock Text="알림음 재생" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Width="120" Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                                                Tag="Highlight.wav" ValueChanged="PreviewSoundSlider_ValueChanged"
                                                Value="{Binding Settings.HighlightAlertVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding Settings.HighlightAlertVolumePercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="알림 볼륨" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=KeywordTabList, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBox x:Name="KeywordBox"
                                 Text="{Binding Settings.KeywordInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputTextBoxStyle}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 Width="606"
                                 HorizontalAlignment="Left"
                                 Height="80"/>
                        <TextBlock Text="@단어1 @단어2 형식 · 입력 즉시 적용됩니다" Style="{StaticResource SettingsRowHintStyle}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- ===================== 추가 기능: 경험치 추적 ===================== -->
                    <StackPanel x:Name="AddonExpPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="ExpTabTracker" Content="일반" IsChecked="True" GroupName="ExpTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="ExpTabLowEff" Content="저효율 알림" GroupName="ExpTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ExpTabTracker, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.ShowExpTracker, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="경험치 추적" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="화면 상단에 실시간 획득량 표시"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="exp.tracker" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding EnableExperienceLimitAlert, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="경험치 누적 알림" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="exp.cumulative_alert" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowWideStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <TextBox Width="110" Text="{Binding ExperienceLimitTotalExp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource SettingsRowInputStyle}" Margin="0,0,6,0"
                                                 PreviewTextInput="ExperienceLimitExp_PreviewTextInput" DataObject.Pasting="ExperienceLimitExp_Pasting"/>
                                        <Button Content="적용" Width="56" Height="26" Padding="0" Command="{Binding ApplyExperienceLimitStateCommand}" Style="{StaticResource SecondaryButtonStyle}" FontSize="12"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="현재 누적 경험치(억)" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="exp.current_total" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="40" Value="{Binding ExperienceAlertFontSize, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                        <TextBlock Text="{Binding ExperienceAlertFontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="누적 알림 폰트 크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="exp.loweff_font" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ExpTabLowEff, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.IsExpAlarmEnabled}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="저효율 알림" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="exp.loweff" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="70" Text="{Binding ExpAlarmThresholdMan, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="기준치(만 EXP 미만)" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="exp.loweff_threshold" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Width="120" Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                                                Tag="EXPBuffCheck.wav" ValueChanged="PreviewSoundSlider_ValueChanged"
                                                Value="{Binding Settings.ExpBuffAlertVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding Settings.ExpBuffAlertVolumePercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="알림 볼륨" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- ===================== 추가 기능: 던전 도우미 ===================== -->
                    <StackPanel x:Name="AddonDungeonPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="DungeonTabRune" Content="룬 경험치·테시스 코어" IsChecked="True" GroupName="DungeonTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"
                                             ToolTip="룬 경험치 던전 및 테시스 코어 던전"/>
                                <RadioButton x:Name="DungeonTabAbyss" Content="어비스" GroupName="DungeonTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="DungeonTabEclipse" Content="이클립스" GroupName="DungeonTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="DungeonTabAbandon" Content="어밴던로드" GroupName="DungeonTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="DungeonTabCraving" Content="갈망하는 즐거움" GroupName="DungeonTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                            </StackPanel>
                        </Border>

                        <!-- 룬 경험치 던전 및 테시스 코어 던전 -->
                        <StackPanel Visibility="{Binding IsChecked, ElementName=DungeonTabRune, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.UseMagicCircleAlert}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="웨이브 종료 알림" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.wave_end" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Width="120" Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                                                Tag="Wave.wav" ValueChanged="PreviewSoundSlider_ValueChanged"
                                                Value="{Binding Settings.MagicCircleAlertVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding Settings.MagicCircleAlertVolumePercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="웨이브 볼륨" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <!-- 어비스 -->
                        <StackPanel Visibility="{Binding IsChecked, ElementName=DungeonTabAbyss, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.EnableReflectionPatternAlert}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="반사 패턴 알림" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.abyss_reflect" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Width="120" Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                                                Tag="Reflection.wav" ValueChanged="PreviewSoundSlider_ValueChanged"
                                                Value="{Binding Settings.ReflectionPatternAlertVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding Settings.ReflectionPatternAlertVolumePercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="반사 패턴 볼륨" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.EnableTreasuryGoldCountAlert, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="보물창고 금화 카운트" Style="{StaticResource SettingsRowLabelStyle}"
                                                   ToolTip="심연의 보물창고 입장 후 금화 주머니 획득 개수를 알림 창으로 표시"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.treasury_gold" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <!-- 이클립스 -->
                        <StackPanel Visibility="{Binding IsChecked, ElementName=DungeonTabEclipse, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.ShowEtosDirectionAlert}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="에토스 방향 알림" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.etos_direction" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.ShowRecaptureSupplyPadOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="보급품 탈환 발판" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.recapture_pad_order" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="70" Text="{Binding Settings.RecaptureSupplyPadOrderDurationSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}"/>
                                    <TextBlock Text="발판 창 유지 시간(초)" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="1~300초"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.ShowRecaptureSupplyMap, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="보급품 탈환 미니 지도" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.recapture_map" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <!-- 어밴던로드 -->
                        <StackPanel Visibility="{Binding IsChecked, ElementName=DungeonTabAbandon, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.EnableAbandonRoadCountAlert, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="어밴던로드 입장 횟수" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.abandon_count" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.ShowAbandonRoadSummaryWindow, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="어밴던로드 통계" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.abandon_gold" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="40" Value="{Binding DungeonCountDisplayFontSize, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                        <TextBlock Text="{Binding DungeonCountDisplayFontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="알림 창 폰트 크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="70" Text="{Binding Settings.AbandonRoadCountAlertDurationSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}"/>
                                    <TextBlock Text="알림 창 지속 시간(초)" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="1~300초"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <!-- 갈망하는 즐거움 -->
                        <StackPanel Visibility="{Binding IsChecked, ElementName=DungeonTabCraving, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.EnableCravingPleasureCountAlert, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="갈망하는 즐거움 입장 횟수" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="dungeon.craving_count" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="40" Value="{Binding CravingPleasureCountFontSize, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                        <TextBlock Text="{Binding CravingPleasureCountFontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="알림 창 폰트 크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <TextBox DockPanel.Dock="Right" Width="70" Text="{Binding Settings.CravingPleasureCountAlertDurationSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsRowInputStyle}"/>
                                    <TextBlock Text="알림 창 지속 시간(초)" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="1~300초"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- ===================== 추가 기능: 아이템 알림 ===================== -->
                    <StackPanel x:Name="AddonItemPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="ItemTabAlert" Content="획득 알림" IsChecked="True" GroupName="ItemTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="ItemTabFilter" Content="알림 필터" GroupName="ItemTabs" Checked="AddonSubTab_Checked" Style="{StaticResource SettingsTabStyle}"/>
                                <Button Style="{StaticResource HelpButtonStyle}" Tag="item.filter" Click="Help_Click" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ItemTabAlert, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.ShowItemDropAlert}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="아이템 획득 알림" Style="{StaticResource SettingsRowLabelStyle}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Tag="item.drop_alert" Click="Help_Click"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Width="120" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                                                Tag="drop_low.mp3" ValueChanged="PreviewSoundSlider_ValueChanged"
                                                Value="{Binding Settings.ItemDropAlertVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding Settings.ItemDropAlertVolumePercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="알림 볼륨" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Minimum="10" Maximum="40" Value="{Binding ItemDropToastFontSize, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                        <TextBlock Text="{Binding ItemDropToastFontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="알림 폰트 크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ItemTabFilter, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="기본 목록에서 알림 받을 항목을 사용자 정의 목록으로 옮긴 뒤 적용하세요." Style="{StaticResource SettingsRowHintStyle}" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Button Content="기본" Command="{Binding SelectDefaultDropFilterCommand}" Width="64" Margin="0,0,6,0" Style="{StaticResource DropFilterButtonStyle}"/>
                            <Button Content="사용자 정의" Command="{Binding SelectCustomDropFilterCommand}" Width="92" Style="{StaticResource DropFilterButtonStyle}"/>
                        </StackPanel>

                        <Grid Width="606" HorizontalAlignment="Left">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UseCustomDropItemFilter}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="42"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="170"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="기본 목록" Foreground="{DynamicResource OverlayInfoTextBrush}" FontSize="11" Margin="0,0,0,4"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="사용자 정의 목록" Foreground="{DynamicResource OverlayInfoTextBrush}" FontSize="11" Margin="0,0,0,4"/>

                            <ListBox x:Name="DefaultDropItemsListBox" Grid.Row="1" Grid.Column="0"
                                     ItemsSource="{Binding DefaultDropItems}" SelectionMode="Extended"
                                     Background="{DynamicResource InputFieldBackgroundBrush}" BorderBrush="{DynamicResource OverlayStrongBorderBrush}"
                                     Foreground="{DynamicResource TextBrush}" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Foreground="{Binding Foreground}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="5,0">
                                <Button Content="&gt;&gt;" Click="MoveDropItemsToCustom_Click" Margin="0,0,0,8" Style="{StaticResource DropFilterButtonStyle}"/>
                                <Button Content="&lt;&lt;" Click="MoveDropItemsToDefault_Click" Style="{StaticResource DropFilterButtonStyle}"/>
                            </StackPanel>

                            <ListBox x:Name="CustomDropItemsListBox" Grid.Row="1" Grid.Column="2"
                                     ItemsSource="{Binding CustomDropItems}" SelectionMode="Extended"
                                     Background="{DynamicResource InputFieldBackgroundBrush}" BorderBrush="{DynamicResource OverlayStrongBorderBrush}"
                                     Foreground="{DynamicResource TextBrush}" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Foreground="{Binding Foreground}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="적용" Command="{Binding ApplyCustomDropItemFilterCommand}" Width="64" Margin="0,0,6,0" Style="{StaticResource DropFilterButtonStyle}"/>
                                <Button Content="불러오기" Command="{Binding LoadCustomDropItemFilterCommand}" Width="70" Margin="0,0,6,0" Style="{StaticResource DropFilterButtonStyle}"/>
                                <Button Content="저장" Command="{Binding SaveCustomDropItemFilterCommand}" Width="64" Style="{StaticResource DropFilterButtonStyle}"/>
                            </StackPanel>
                        </Grid>
                        <TextBlock Text="{Binding CustomDropItemStatus}" Style="{StaticResource SettingsRowHintStyle}" Margin="0,6,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- ===================== 추가 기능: 버프 추적 ===================== -->
                    <StackPanel x:Name="AddonBuffPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="일반" IsChecked="True" GroupName="BuffTabs" IsHitTestVisible="False" Style="{StaticResource SettingsTabStyle}"/>
                                <Button Style="{StaticResource HelpButtonStyle}" Tag="buff.tracker" Click="Help_Click" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <WrapPanel>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.EnableBuffTrackerAlert}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <TextBlock Text="버프 추적" Style="{StaticResource SettingsRowLabelStyle}" ToolTip="버프 시간 추적을 켜고 끕니다"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding Settings.EnableBuffTrackerEndSound}" IsEnabled="{Binding Settings.EnableBuffTrackerAlert}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                    <TextBlock Text="버프 종료 사운드" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsRowStyle}">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Slider Width="120" Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                                                IsEnabled="{Binding Settings.EnableBuffTrackerAlert}"
                                                Tag="BuffCheck.wav" ValueChanged="PreviewSoundSlider_ValueChanged"
                                                Value="{Binding Settings.BuffTrackerEndSoundVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding Settings.BuffTrackerEndSoundVolumePercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="종료 사운드 볼륨" Style="{StaticResource SettingsRowLabelStyle}"/>
                                </DockPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>

                    <!-- ===================== 추가 기능: 필드 보스 ===================== -->
                    <StackPanel x:Name="AddonBossPanel" Visibility="Collapsed">
                        <Border Style="{StaticResource SettingsTabStripStyle}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="필드 보스 알림" IsChecked="True" GroupName="BossTabs" IsHitTestVisible="False" Style="{StaticResource SettingsTabStyle}"/>
                                <Button Style="{StaticResource HelpButtonStyle}" Tag="boss.alert" Click="Help_Click" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ItemsControl ItemsSource="{Binding BossAlarmCards}" Width="606" HorizontalAlignment="Left">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="3"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="10" Margin="0,0,10,10"
                                            Background="{DynamicResource OverlayCardBackgroundBrush}"
                                            BorderBrush="{DynamicResource OverlayStrongBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="3">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" FontSize="14" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding ScheduleText}" Foreground="{DynamicResource OverlayHintTextBrush}" FontSize="11" Margin="0,3,0,10" TextWrapping="Wrap"/>
                                            <Grid Margin="0,0,0,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="3분 전" Foreground="{DynamicResource TextBrush}" FontSize="12" VerticalAlignment="Center"/>
                                                <CheckBox Grid.Column="1" IsChecked="{Binding Alert3MinutesBefore, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                            </Grid>
                                            <Grid Margin="0,0,0,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="1분 전" Foreground="{DynamicResource TextBrush}" FontSize="12" VerticalAlignment="Center"/>
                                                <CheckBox Grid.Column="1" IsChecked="{Binding Alert1MinuteBefore, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                            </Grid>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="5초 전" Foreground="{DynamicResource TextBrush}" FontSize="12" VerticalAlignment="Center"/>
                                                <CheckBox Grid.Column="1" IsChecked="{Binding AlertAtSpawn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                            </Grid>
                                            <!-- 입장 가능 시간이 있는 보스(혼란한 대지 4분·파멸의 기원 6분·JSON entryMinutes): 등장 후 카운트다운 -->
                                            <Grid Margin="0,6,0,0" Visibility="{Binding HasEntryCountdown, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="입장 시간 카운트" Foreground="{DynamicResource TextBrush}" FontSize="12" VerticalAlignment="Center" ToolTip="{Binding EntryCountdownToolTip}"/>
                                                <CheckBox Grid.Column="1" IsChecked="{Binding EntryCountdown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Border Style="{StaticResource SettingsRowStyle}" BorderThickness="0">
                            <DockPanel>
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                    <Slider Width="120" Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                                            Tag="Arkan.wav" ValueChanged="PreviewSoundSlider_ValueChanged"
                                            Value="{Binding Settings.BossAlertVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBlock Text="{Binding Settings.BossAlertVolumePercent, StringFormat={}{0:F0}%}" Foreground="{DynamicResource TextBrush}" Width="34" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                                <TextBlock Text="알림 볼륨" Style="{StaticResource SettingsRowLabelStyle}"/>
                            </DockPanel>
                        </Border>
                        <Border Style="{StaticResource SettingsRowStyle}" BorderThickness="0">
                            <DockPanel>
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                    <Slider Minimum="10" Maximum="40" Value="{Binding BossAlertToastFontSize, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Width="140"/>
                                    <TextBlock Text="{Binding BossAlertToastFontSize, StringFormat={}{0:F0}}" Foreground="{DynamicResource TextBrush}" Width="26" TextAlignment="Right" VerticalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                                <TextBlock Text="알림 창 폰트 크기" Style="{StaticResource SettingsRowLabelStyle}"/>
                            </DockPanel>
                        </Border>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
